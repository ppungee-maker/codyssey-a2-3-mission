# [Project C] AI 기반 고객 리뷰 감정 분석 대시보드

> 코디세이 `AI 활용 학습 (AI Native Advanced)` 과정 · `codyssey mission` API로 원문 그대로 발췌.

### 1. 미션 소개

리뷰 1,000건을 읽으려면 사람이 며칠이 걸립니다. AI를 쓰면 몇 분 안에 끝납니다. 그러나 단순 분류하는 것에서 끝나면 의사결정에 써먹기가 어렵습니다. 시간에 따른 감정 변화 추이, 불만 키워드, 별점과 감정의 상관관계까지, 비즈니스 인사이트로 연결되는 대시보드를 만듭니다.

고객 리뷰는 제품과 서비스의 품질을 가늠하는 가장 직접적인 지표입니다. 하지만 수백, 수천 건의 리뷰를 사람이 일일이 읽고 분석하기란 현실적으로 어렵습니다. 이때 AI를 활용하면 대량의 텍스트에서 감정을 자동 분류하고, 핵심 키워드와 트렌드를 빠르게 파악할 수 있습니다.

이 미션에서는 고객 리뷰를 수집하고 AI로 감정 분석한 뒤 대시보드 형태의 시각화 리포트를 생성하는 CLI 기반 서비스를 구현합니다. 단순히 감정을 분류하는 데 그치지 않고, 시간에 따른 감정 변화 추이, 주요 불만/칭찬 키워드 추출, 별점과 감정의 상관관계 등 비즈니스 의사결정에 활용 가능한 인사이트를 도출합니다.

### 2. 최종 결과물

다음 기능이 정상 동작하는 CLI 기반 Python 애플리케이션 1개를 완성한다.

1. 리뷰 데이터 수집 및 저장 — 외부 파일(CSV/Excel)을 통해 리뷰 데이터를 수집하고, raw/clean 분리 저장이 동작한다. 중복 리뷰는 설정에 따라 skip 또는 upsert 처리된다.
2. AI 기반 감정 분석 — 저장된 리뷰 텍스트를 AI API에 전달하여 감정(긍정/부정/중립)과 감정 점수를 분석하고 저장한다. 사용자가 CLI에서 분석 대상과 옵션을 지정할 수 있다.
3. AI 기반 키워드 및 요약 추출 — 리뷰 전체 또는 감정별 리뷰에서 주요 키워드, 빈출 불만/칭찬 사항을 추출한다. 기간별 또는 제품별 리뷰 요약을 생성할 수 있다.
4. 데이터 조회 및 검색 — 리뷰 목록 조회, 상세 조회, 조건 필터링(감정, 별점, 기간), 페이지네이션이 CLI에서 동작한다. 감정 분석 결과와 함께 조회할 수 있다.
5. 대시보드 시각화 및 리포트 생성 — 감정 분포, 시간별 추이, 별점-감정 상관관계 등을 차트로 시각화한다. 품질 지표와 TOP N 집계가 포함된 종합 리포트를 생성한다. 결과를 CSV, 엑셀 파일로 내보낼 수 있다.

### 3. 과제 목표

- 다양한 형식의 리뷰 데이터를 읽어 들이고 정제하는 과정을 설명할 수 있다.
- AI API를 호출하여 텍스트의 감정을 분석하고, 결과를 구조화하여 저장하는 흐름을 설명할 수 있다.
- 감정 분석 결과를 집계하고 matplotlib으로 다양한 차트를 생성하는 과정을 설명할 수 있다.
- 분석 결과를 비즈니스 관점에서 해석하고 인사이트를 도출하는 방법을 설명할 수 있다.

### 4. 기능 요구 사항

1. **CLI 설계**: argparse 기반 서브커맨드. 필수: `import`/`clean`/`analyze`/`extract`/`list`/`show`/`stats`/`dashboard`/`export`. 각 서브커맨드는 필요한 옵션(`--file`/`--sentiment`/`--rating`/`--format` 등) 지원.
2. **리뷰 데이터 수집**: CSV/Excel 파일에서 읽기. 필수: 리뷰 텍스트. 선택: 별점(1~5)/작성일/제품명. raw 저장소(SQLite/JSONL)에 저장.
3. **데이터 정제**: 필수필드 검증, 텍스트 정규화, 별점 범위 검증, 날짜형식 통일, 짧은 리뷰 필터링. 중복 skip/upsert. clean 저장소 별도.
4. **AI 감정 분석**: 감정(긍정/부정/중립) + 신뢰도 점수(0.0~1.0). 대상 옵션(`--all`/`--id`/`--unanalyzed`). 실패시 로깅 후 스킵, 이미 분석된 건 기본 스킵.
5. **AI 키워드/요약 추출**: 조건별(기간/감정/제품) 종합. 항목 2개 이상: 긍정/부정 키워드, 전체요약, 개선제안. 별도 저장.
6. **데이터 조회**: list(필터+페이지네이션+정렬), show(원문+분석결과), stats(총건수/감정별비율/평균별점).
7. **대시보드 시각화**: matplotlib 최소 3종(감정분포, 시간별추이, 별점별감정분포). 한글폰트, PNG.
8. **리포트 생성**: 품질지표 2개+, TOP N 1개+, AI추출결과 포함. 콘솔+파일(TXT/MD).
9. **데이터 내보내기**: CSV/JSONL/Excel 중 최소 2개. 필터(`--sentiment`/`--rating-min`).
10. **설정 및 로깅**: `config.json`(API키/중복정책/시각화옵션). `logging` 모듈 INFO/WARNING/ERROR.
11. **데이터 저장**: 반드시 영구 저장소(SQLite/JSONL). 메모리만 사용 금지.
12. **코드 구조**: 단일 파일 금지, 최소 4개 이상 모듈 분리.
13. **샘플 데이터**: 테스트용 샘플 리뷰(최소 30건 이상) CSV로 함께 제출.

### 5. 보너스 과제

- 다국어 감정 분석 지원(한국어 외 영어)
- 감정 변화 알림 기능(최근 N일 부정비율 급증시 경고)
- HTML 대시보드 생성(단일 HTML 파일)
- 제품/카테고리별 비교 분석 기능
